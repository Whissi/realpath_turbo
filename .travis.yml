sudo: required
dist: trusty

language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6

before_install: phpize

install: ./configure --prefix=/usr && sudo make install && echo 'extension = turbo_realpath.so' >> /etc/php5/cli/php.ini

#script: make test
script: php --rextinfo turbo_realpath
